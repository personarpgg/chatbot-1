# 이 파일을 secrets.toml 로 복사한 뒤 키를 입력하면
# 앱 실행 시 API Key 입력창 없이 자동으로 로드됩니다.
#
# 복사 방법: secrets.toml.example → secrets.toml
# (secrets.toml은 .gitignore에 포함되어 GitHub에 올라가지 않습니다)

OPENAI_API_KEY = "sk-..."
